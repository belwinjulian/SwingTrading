---
quick_id: 260503-q01
slug: fix-dat-08-stooq-splits
description: "Fix DAT-08 gap: add fetch_splits + write_splits_atomic to the Stooq else-branch in run_with_breaker (ohlcv.py ~line 280)"
created: 2026-05-03
wave: 1
must_haves:
  truths:
    - "run_with_breaker Stooq else-branch writes splits.parquet for every ticker it handles"
    - "yfinance success branch is unchanged"
  artifacts:
    - path: src/screener/data/ohlcv.py
      pattern: "write_splits_atomic"
      count_min: 2
---

# Quick Task 260503-q01: Fix DAT-08 — Stooq path missing splits write

## Objective

Close the DAT-08 gap identified in 02-REVIEW.md (CR-01) and 02-VERIFICATION.md:
the Stooq circuit-breaker else-branch in `run_with_breaker` writes `prices.parquet`
but never writes `splits.parquet`. Any ticker routed through Stooq gets no splits
ledger, causing Phase 6 pattern detection to raise `FileNotFoundError`.

## Tasks

### Task 1 — Add `fetch_splits` + `write_splits_atomic` to the Stooq else-branch

**File:** `src/screener/data/ohlcv.py` lines 277-281

**Action:** After `write_ohlcv_atomic(ticker, df)` in the Stooq try-block, add:
```python
splits_df = fetch_splits(ticker)
write_splits_atomic(ticker, splits_df)
```

Both `fetch_splits` (defined in the same file) and `write_splits_atomic` (imported
from `screener.persistence`) are already available. `make_empty_splits()` is the
fallback when yfinance actions returns nothing — `fetch_splits` already handles
that internally.

**Verify:** `write_splits_atomic` appears ≥2 times in the file (once yf branch, once Stooq branch).

**Done:** `git log --oneline -1` shows commit referencing `260503-q01` or `fix-dat-08`.

### Task 2 — Run tests to confirm no regression

**Action:** `uv run pytest -q --tb=short`
**Verify:** All tests pass (39+2 expected).
**Done:** Exit code 0.
